package com.quickparkassist.model;import org.junit.jupiter.api.Test;import static org.junit.jupiter.api.Assertions.assertEquals;class AddonServiceBookingTest {    @Test    void testGetUserId() {        // Arrange        Long expectedUserId = 123L;        AddonServiceBooking booking = new AddonServiceBooking();        booking.setUserId(expectedUserId); // Set the user ID using the setter        // Act        Long actualUserId = booking.getUserId(); // Get the user ID using the getter        // Assert        assertEquals(expectedUserId, actualUserId, "The user ID should match the expected value.");    }    @Test    void testSetUserId() {        // Arrange        AddonServiceBooking booking = new AddonServiceBooking();        Long expectedUserId = 456L; // Another example user ID        // Act        booking.setUserId(expectedUserId); // Set user ID via setter        // Assert        assertEquals(expectedUserId, booking.getUserId(), "The user ID should match the expected value.");    }    @Test    void testGettersAndSetters() {        AddonServiceBooking addonServiceBooking = new AddonServiceBooking();                Long bookingId = 1L;        String serviceIDs = "1,2,3";        String quantities = "1,2,1";        // Set values        addonServiceBooking.setAddonServiceBookingID(bookingId);        addonServiceBooking.setServiceIDs(serviceIDs);        addonServiceBooking.setQuantities(quantities);        // Assertions        assertEquals(bookingId, addonServiceBooking.getAddonServiceBookingID());        assertEquals(serviceIDs, addonServiceBooking.getServiceIDs());        assertEquals(quantities, addonServiceBooking.getQuantities());    }    @Test    void testAllArgsConstructor() {        AddonServiceBooking addonServiceBooking = new AddonServiceBooking(1L, "1,2", "1,2");        assertEquals(Long.valueOf(1), addonServiceBooking.getAddonServiceBookingID());        assertEquals("1,2", addonServiceBooking.getServiceIDs());        assertEquals("1,2", addonServiceBooking.getQuantities());    }    @Test    void testNoArgsConstructor() {        AddonServiceBooking addonServiceBooking = new AddonServiceBooking();        assertEquals(null, addonServiceBooking.getAddonServiceBookingID());        assertEquals(null, addonServiceBooking.getServiceIDs());        assertEquals(null, addonServiceBooking.getQuantities());    }}